# =====================================================
# LSTracker — environment template
# Copy this file to ".env" and replace every value below.
# Never commit ".env" (it is in .gitignore).
# =====================================================

# ---- Database ----------------------------------------
# Password used both by the postgres container and the
# Spring Boot application.
POSTGRES_DB=sample_tracker
POSTGRES_USER=appuser
# Replace by a strong password (min 20 chars, mixed)
POSTGRES_PASSWORD=CHANGE_ME_TO_A_STRONG_PASSWORD

# ---- Application -------------------------------------
SERVER_PORT=8050
TZ=Africa/Abidjan

# ---- JWT signing secret ------------------------------
# Generate with:
#   openssl rand -hex 64
# 64 hex chars = 256 bits (HMAC-SHA256) is the minimum.
# 128 hex chars = 512 bits is safer.
# NEVER use the default value committed in application.properties.
JWT_SECRET=CHANGE_ME_RUN_openssl_rand_-hex_64

# Token TTLs (minutes / days)
JWT_EXPIRATION_MINUTES=43200
JWT_REFRESH_TTL_DAYS=60

# ---- DB pool tuning (optional) -----------------------
DB_POOL_MAX=30
DB_POOL_MIN=5
